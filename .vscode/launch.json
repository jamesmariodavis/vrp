{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python module",
            "type": "debugpy",
            "request": "launch",
            // "module": "${input:sheetsBackendModuleRoot}",
            "cwd": "${workspaceRoot}",
            "justMyCode": true,
            "program": "${file}",
            "env": {"PYTHONPATH": "${workspaceRoot}"},
        }
    ]
}